import React from "react";

const NavBar = props => {
  return (
    <div>
      <a href="#">Home</a>
      <a href="#">About</a>
      <a href="#">Space</a>
      <a href="#">Blackhole</a>
    </div>
  );
};

export default NavBar;
