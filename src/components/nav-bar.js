import React from "react";

const NavBar = props => {
  return (
    <div>
      <a href="#">Home</a>
      <a href="#">About</a>
      <a href="#">Space</a>
      <a href="#">Black-hole</a>
    </div>
  );
};

export default NavBar;
